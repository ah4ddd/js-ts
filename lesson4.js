// Strings & Template Literals

const userName = "Ahad  ";
const work = "Coder";

// Template Literals (`backticks` > insert expression using ${})
console.log(`Hello ${userName.trim()} ${work}`);

// String Properties
// console.log(name.length);
// name.toUpperCase() & console.log(name.toLowerCase());

const message = "I am learning JavaScript";
// Check whether a string contains something
console.log(message.includes("JavaScript"));

const language = "JavaScript";

console.log(language.slice(4, 10), language.slice(0, 4));
